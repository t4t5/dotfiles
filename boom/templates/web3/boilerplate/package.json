{
  "name": "__name__",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "npm run buildcontracts && npm run deploycontracts",
    "buildcontracts": "TYPECHAIN_PATH='../web/src/contracts' turbo run build --filter=contracts",
    "deploycontracts": "turbo run deploy --filter=contracts",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "npm@8.1.0"
}
